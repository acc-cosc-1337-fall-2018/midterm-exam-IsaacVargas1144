#include "dive.h"

class Dive
{
public:
	d = 0; //depth in feet
	s = 0; //starting pressure
	f = 0; //final pressure
	t = 0; //time in minutes

private:
	Dive.get_sacr()
	{
		sacr = 33(s - f) / t(d + 33);
		return sacr
	}

};